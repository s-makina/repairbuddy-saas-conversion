# Base URL of the Laravel backend (no trailing slash)
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000

# App domain used to construct workspace URLs (no protocol, no trailing slash)
# e.g. 99smartx.com -> workspace URL becomes: {slug}.99smartx.com
NEXT_PUBLIC_APP_DOMAIN=99smartx.com
